"use client";

// The JS-based custom cursor was causing lag on your machine.
// We are disabling it to ensure the highest performance. 
// A laggy cursor ruins the luxury feel, so native is better!
export default function Cursor() {
  return null;
}
